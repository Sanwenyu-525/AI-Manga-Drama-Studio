# CONTEXT — 术语表

> 纯术语表，不含实现细节。术语在拷问/规划会中敲定后即时录入。

## 真实链路验证（Real Chain Validation）

用真实 LLM 与真实图像 Provider（而非 fake/mock）端到端运行核心生产链路、以结构化证据校准路线图的验证型 Sprint。产出是验证报告与 backlog 重排提案，不是新功能。

## 核心段（Core Segment）

漫剧生产的最短可信链路：导入小说 → 分析预览 → 确认创建 Scene → 生成分镜 Shot → 图像生成 → 版本审阅。排片、配音、渲染不在核心段内。

## 硬阻塞（Hard Blocker）

不修复就无法继续核心段流程的缺陷。验证期间仅硬阻塞允许当场修复（最小修法 + 回归测试）；其余问题一律只记录不修。

## 首次可用率（First-pass Usability Rate）

首次生成即被快评为「可用」的图像张数占总首次生成张数的比例。核心段图像质量的主指标；阈值与解读见 sprint-04 规划 §4.3。

## 快评（Quick Review）

对每张生成图像的三维度轻量评分：景别符合度、角色一致性、整体可用性（各 1–5 分）+ 三态判定（可用/需重生成/废弃）+ 失败模式枚举标签。

## 双引擎对跑（Dual-engine Run）

同一批 shots、同一组 prompt，在 Agnes（云端）与本地 ComfyUI 两个图像引擎各生成一遍并分别快评。目的：把「prompt 质量问题（Studio 可控）」与「引擎/模型质量问题（需换 provider 或上参考图机制）」分开归因。
